<table class="table table-striped table-bordered table-condensed" id="proposals_table">
	<thead>
		<tr>
			<th>Proposal</th>
			<th>Title</th>
			<th>Proposal Date</th>
			<th>Award</th>
			<th>Last Viewed</th>
			<th>Panel/s (Program Officer/s)</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
	</tbody>
</table>

<script type="text/template" id="template_proposals_listitem">
<td>{{nsf_id}}</td>
<td>{{title}}</td>
<td>{{date}}</td>
<td>{{amount}}</td>
<td>{{lastviewed}}</td>
<td>{{panels}}</td>
<td>
<% if current_user.role?(:admin) %>
	<% if @user.nil? %>
		<strong>Assigned to: </strong> {{users}}<br /><br />
		<a href="#" id="proposals_refresh_{{id}}">Refresh</a><br />
		<%= link_to 'Show', 'proposals/{{id}}' %><br />
		<%= link_to 'Edit', 'proposals/{{id}}/edit' %><br />
		<a href="#" id="proposals_remove_{{id}}"></i>Remove</a>
	<% else %>
		<a href="#" id="proposals_refresh_{{id}}"><i class="icon-refresh"></i>Refresh</a><br />
		<a href="#" id="proposals_unassign_{{id}}"><i class="icon-remove"></i>Remove</a></td>
	<% end %>
<% else %>
	<a href="proposals/{{id}}">Review</a></td>
<% end %>
</script>

